import React from 'react';
import RestaurantList from './RestaurantList';

const Home = () => {
  return (
    <div>
      <h2>Home</h2>
      <RestaurantList/>
    </div>
  );
};

export default Home;
